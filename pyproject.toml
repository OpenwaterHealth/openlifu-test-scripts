[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "openlifu-verification-tests"
version = "1.0.0"
requires-python = ">=3.10,<=3.12.13"
dependencies = [
  "openlifu-sdk",  
]

[tool.hatch.build.targets.wheel]
packages = ["openlifu-verification-tests"]